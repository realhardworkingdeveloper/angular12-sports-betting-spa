import { PickOrderPipe } from './pick-order.pipe';

describe('PickOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new PickOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
